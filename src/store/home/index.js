const state = {
    imgBackground : [{opacity:0},{opacity:100}],
    imgNum:[[0,1],[1,0]],
    timer: null,
    timerTwo: null,
}
const actions = {
    getChoose({commit}){
        //axios.get("").then()
    }
}

export default{
    state,
    // getters,
    actions,
    // mutations,
    // mudoles
}